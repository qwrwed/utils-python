[build-system]
requires = [
    "setuptools ~= 64.0",
    "wheel",
]

[project]
name="utils_python"
version="1.7.2"
dependencies = [
    "requests ~= 2.31.0",
    "tqdm ~= 4.66.1",
]

[tool.isort]
profile = "black"
