[build-system]
requires = [
    "setuptools",
    "wheel",
]

[project]
name="utils_python"
version="1.14.0"
dependencies = [
    "requests ~= 2.31.0",
    "tqdm ~= 4.66.1",
]

[tool.isort]
profile = "black"
