[build-system]
requires = [
    "setuptools",
    "wheel",
]

[project]
name="utils_python"
version="1.14.2"
dependencies = [
    "requests ~= 2.31",
    "tqdm ~= 4.66",
]

[tool.isort]
profile = "black"
